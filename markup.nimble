# package

version     = "0.0.1"
author      = "bob16795"
description = "Markup in nim"
license     = "MIT"
bin         = @["src/markup"]

# Deps

requires "nim >= 0.10.0"
requires "sdl2_nim >= 2.0.10.0"
