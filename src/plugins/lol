{init}:
	SET testpluginVersion = 0.0.0
	TAG LIN 100
LS: (path)
	CODE: bash
		ls [path]
IMPORTPY: (PYFILE)
	TAG NINDENT
	TAG LOG Import [PYFILE]
	TAG LIN 100
	CODE: sh
		[ -d ./code ] && echo ./code && python ./code/[PYFILE].py
		[ -d ./../code ] && python ./../code/[PYFILE].py
		[ -d ./../../code ] && python ./../../code/[PYFILE].py
	TAG LIN 100
	TAG INDENT